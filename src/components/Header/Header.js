import React from "react";
import "./Header.scss";
export const Header = (props) => {
  return (
    <header className="header">
      <h1>Header</h1>
    </header>
  );
};

Header.propTypes = {};

export default Header;
